lst.forEach(myFunction);
function myFunction(value) {
  if (value>z){ z=value;}
}
let index = lst.indexOf(z);
const winner=nama[index];
const accountno=acc;
// document.getElementById('a').innerHTML = acc;